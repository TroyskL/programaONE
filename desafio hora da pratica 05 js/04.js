function calcularMedia(num1, num2, num3) {
    var soma = num1 + num2 + num3;
    var media = soma / 3;
    return media;
  }

  var resultadoMedia = calcularMedia(10, 15, 20);
  console.log(resultadoMedia);